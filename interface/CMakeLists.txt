# build
SET(TARGET_LIB objinterface)
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR}/../service)
FILE(GLOB_RECURSE SRC *.cc)
ADD_LIBRARY(${TARGET_LIB} OBJECT ${SRC})
TARGET_COMPILE_DEFINITIONS(${TARGET_LIB} PRIVATE
	-DCONFIG_PLUGIN_PATH=\"${plugindir}\")